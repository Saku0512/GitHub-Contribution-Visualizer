# GitHub-Contribution-Visualizer プロジェクト詳細

このドキュメントでは、本プロジェクトの概要、使用技術、および各機能の実装方法について詳細に解説します。

## 1. プロジェクト概要
このアプリケーションは、GitHubのユーザー名を入力することで、過去1年間の活動データ（コントリビューション）を分析し、以下の2点を提供するWebアプリです。

- **開発スタイルの分析**: 活動傾向からその人の「ペルソナ」を判定。
- **3Dアバターの動的生成**: 統計データをパラメータとして、コードベースで独自の3Dモデルを組み立て。

単なる数字の可視化を超え、開発者のアイデンティティをビジュアルとして表現することを目的としています。

---

## 2. 使用技術
モダンで軽量、かつ保守性の高い技術スタックを採用しています。

### バックエンド
- **言語**: Go (Golang) 1.26
- **ライブラリ**: 標準ライブラリ (`net/http`) を中心とした構成
- **API**: GitHub GraphQL API (v4)

### フロントエンド
- **フレームワーク**: Svelte 5
- **ビルドツール**: Vite
- **3Dライブラリ**: Three.js
- **スタイリング**: Vanilla CSS

### インフラ・構成
- **コンテナ化**: Docker / Docker Compose
- **リバースプロキシ**: Traefik (フロントエンドとAPIの統合)

---

## 3. 各機能の実装方法と詳細

### ① 活動データの収集 (Backend)
- **GraphQL連携**: `internal/githubapi` パッケージにて、GitHub GraphQL API を呼び出します。
- **データ取得範囲**: 過去1年間の `contributionCalendar` および、リポジトリごとの「コミット」「プルリクエスト」「Issue」「レビュー」の数。
- **効率化**: 必要な情報を1回のリクエストで取得し、バックエンド側で構造化されたスナップショットに変換します。

### ② 開発スタイルの分析 (Backend)
- **メトリクス計算**: `internal/analysis` パッケージにて、取得したスナップショットから以下の統計を算出。
    - **Weekend Ratio**: 週末の活動比率。
    - **Streak**: 最長連続活動日数。
    - **Dominant Activity**: 最も頻度の高い活動種別。
- **ペルソナ判定**: 計算されたメトリクスに基づき、5つのテンプレートから最適なものを選択します。
    - *Collaboration Captain* (協調型)
    - *Steady Gardener* (継続型)
    - *Weekend Inventor* (週末集中型)
    - *Sprint Crafter* (瞬発型)
    - *Momentum Builder* (標準型)

### ③ 3Dアバターの動的生成 (Frontend)
- **プロシージャル・モデリング**: `AvatarPreview.svelte` 内で Three.js を使用。
- **パラメータマッピング**:
    - **活動日数** → 胴体の高さ（継続力の表現）
    - **最多活動日の数** → 頭部の半径（集中力の表現）
    - **総コントリビューション数** → 背面のフィンの枚数（蓄積量の表現）
    - **活動種別** → 配色の決定（例: PR/レビューが多い場合はオレンジ・ブルー系）
- **リアルタイム性**: APIから返されたJSONデータを即座に3Dのジオメトリパラメータに反映します。

### ④ 統合環境 (Docker/Traefik)
- **シングルオリジン**: Traefik を用いて `/api` をバックエンドへ、それ以外をフロントエンドへ振り分けます。
- **開発体験**: `frontend` は Vite のデーブサーバとして動作し、ホットリロードが有効です。

---

## 4. 機能ごとの使用技術まとめ

| 機能 | 技術 | 役割 |
| :--- | :--- | :--- |
| **API Server** | Go | バックエンド基盤、HTTPルーティング |
| **Data Fetching** | GraphQL | GitHubデータの取得 |
| **Analysis** | Go | 統計アルゴリズム、ペルソナロジック |
| **3D Rendering** | Three.js | ブラウザ上での動的なモデル生成 |
| **Reactive UI** | Svelte | 高速なUI更新と状態管理 |
| **Infrastructure** | Docker / Traefik | 実行環境の分離とリバースプロキシ |
